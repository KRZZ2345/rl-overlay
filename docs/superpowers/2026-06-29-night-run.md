# Run de nuit — 2026-06-29 (mode auto, utilisateur endormi)

Consigne : préparer la big update de demain. Construire le **gratuit/réversible**,
**ne pas releaser** (la release se fera demain après validation). Ne pas s'arrêter sur
les décisions : les **noter** ici, faire un **résumé au réveil**.

## ⏰ RÉSUMÉ POUR LE MATIN (lire ça en premier)
*(mis à jour à chaque itération)*
- Terrain freemium posé : `entitlement.js` (tout gratuit, isPremium=true), tags free/premium.
- Roadmap détaillée : `docs/superpowers/specs/2026-06-29-freemium-big-update-design.md` (§9 catalogue).
- Réglages : taille, opacité, toggles **série / ±MMR / musique** (page Réglages, sliders/toggles).
- **Diagnostic** (version, log/tracker, ouvrir-logs, forcer-maj), **fallback log OneDrive**.
- **Journal des matchs** + **Historique 📈** (W/L jour, win-rate, net MMR, derniers matchs, **sparkline MMR**).
- Terrain freemium `entitlement.js` (tout gratuit) + roadmap (spec §9).
- Bouton **Réinitialiser les réglages** + scaffold **i18n** (FR/EN, pas câblé).
- **Rien n'est releasé** : sur `main`, à tester en jeu + release (`release.ps1 -Bump minor`). 88 tests OK.
- **Run de nuit terminé** : terrain gratuit sûr complet ; le reste attend tes décisions (§DÉCISIONS) / supervision.
- Décisions qui t'attendent : voir §DÉCISIONS (paiement, split, repo privé?, release auto?).

## ✅ DONE
- `entitlement.js` + tests (3 pass). Scaffold dormant.
- Roadmap/catalogue d'idées (spec §9).
- **Réglages gratuits batch 1** : Taille (50-150%), Opacité (40-100%), toggle Série 🔥.
  Wirés config + IPC (`set-overlay-flag` généralisé bool+num) + renderer + page Réglages.
  Vérifié : DOM overlay (scale/opacité/streak appliqués) + DOM Hub (sliders/toggles).
  72 tests pass. **Pas releasé.**
- **Panneau Diagnostic** (Réglages > Diagnostic) : version, mode, dossier config, log RL
  trouvé?, en-partie, stats, maj téléchargée + boutons "Ouvrir les logs" / "Vérifier la maj".
  IPC get-diagnostics/open-logs-folder/force-update-check. Vérifié DOM (7 lignes, boutons).
  72 tests pass. **Pas releasé.**

## 📋 TODO (ordre)
NB: l'essentiel du terrain gratuit sûr est posé. Les items restants sont soit gros/risqués
seul (i18n, OBS), soit fiddly (W-L). À faire plutôt supervisé / après tes décisions §DÉCISIONS.
1. Toggle W-L : marquer chaque forme (data-wl) + gérer séparateurs (f5 sep5, marquee ` · `).
2. i18n FR/EN : extraire libellés overlay+hub dans un dict + toggle. Gros refactor, à superviser.
3. Mode source OBS (page localhost) — gros, vrai argument premium.
4. Petits sûrs si besoin : taille MMR (multiplicateur par forme), export/import config (dialog).
NB: le terrain "valeur premium" (Historique + graphe) est posé. Reste surtout du confort
+ la décision monétisation (§DÉCISIONS) avant d'aller plus loin sur le payant.

## 🟠 DÉCISIONS À VALIDER (je continue sans bloquer)
- **Monétisation** : provider (Lemon Squeezy / Gumroad / Stripe) ? prix ? online vs offline ?
  repo privé/closed-source ? (cf. spec §6). → JE NE BRANCHE PAS le paywall sans ça.
- **Split gratuit/premium** exact (proposition spec §5 : 3 thèmes / 2 formes gratuits).
- **Release** : OK pour que je release la big update tout seul, ou tu valides d'abord ?
  (Par défaut : je NE release PAS, je laisse sur main.)

## ✅ DONE (suite)
- Fallback chemin log OneDrive (`logPathCandidates` + probe) + tests.
- Journal des matchs `lib/matchlog.js` (pur, testé) + `matches.json` chargé au boot,
  entrée écrite quand le MMR change après une fin de match.

- **Panneau Historique 📈** (Hub) : stats du jour + 12 derniers matchs, IPC get-matches. Vérifié DOM.
- **Sparkline MMR** (`lib/sparkline.js` pur+testé) dans l'Historique : courbe des 30 derniers MMR,
  verte/rouge selon tendance. Vérifié DOM.
- **Toggle ±MMR (delta)** sur toutes les formes (showDelta). Vérifié DOM.
- **Bouton "Réinitialiser les réglages"** (Réglages) : remet halo/série/±MMR/musique/taille/opacité
  par défaut, applique en direct + resync. Vérifié DOM.

## ✅ FIN DU RUN DE NUIT — terrain gratuit sûr COMPLET
- `lib/i18n.js` scaffold (dict FR/EN + `t()` pur) + tests — committé.
- **Plus rien de sûr/utile à construire seul** : le reste demande ta supervision ou tes
  décisions (voir §DÉCISIONS) :
  - Toggle W-L : fiddly (9 formes, séparateurs) — à faire posément, pas la nuit.
  - i18n wiring : gros refactor de toutes les chaînes — supervisé.
  - Mode OBS : gros, vrai argument premium — supervisé.
  - **Paywall/monétisation** : bloqué sur tes décisions (provider, prix, repo privé?).
- Loop ralenti (réveil 1h) pour ne pas gaspiller — relance-moi quand tu veux pour la suite.

## 🧪 État vérif
- Tests : 88 pass. Pas de release. Tout sur `main`. Pour publier : `release.ps1 -Bump minor`.
